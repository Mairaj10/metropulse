name: 'metropulse_dbt'
version: '1.0.0'

profile: 'metropulse_dbt'

vars:
  delay_tolerance_seconds: 60

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]